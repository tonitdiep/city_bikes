#!/usr/bin/env ruby   tells files we're running ruby
#starter is the executables

require_relative '../lib/environment'
puts "Hello"

#initialize cli class & call the start method aka #call cli class
#very little code to start your program
#ruby bin/city_bikes

CLI.new.start
cli_instance = CLI.new
cli_instance.start